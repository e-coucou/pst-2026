# Tableau de Bord

**Propriétaire :** e-Coucou
**Statut :** En cours - Phase de structuration
**Accès :** Page réservée aux membres "Super" uniquement

---

## 🔴 PRIORITÉS CRITIQUES
- [ ] **Data :** Vérifier l'intégrité des schémas de base de données pour la version 2026.
- [ ] **Sécurité :** Vérifier les policies RLS de `activity_logs`, `photos_import` et la fonction `get_popularity_stats` dans un schéma SQL versionné (rien n'est commité à ce jour, tout vit dans le dashboard Supabase).
- [ ] **Live :** Graphique ELO en bas de `/app/live` — le tooltip ne fonctionne pas correctement. Mis de côté pour l'instant.
- [ ] **Sécurité :** `lib/auth-actions.ts` non importé nulle part (`verifyInvitationCode` semble remplacée par le RPC Supabase `verify_invitation_code`, `isAdmin` inutilisée aussi) — **à revérifier avant suppression**, sujet sensible car même thématique que la faille d'inscription Google OAuth déjà corrigée (code d'invitation non revérifié côté serveur). Ne pas supprimer tant que ce point n'est pas confirmé.

## 🛠️ EN COURS (Sprint Actuel)
- [ ] **Archivage :** À la fin du tournoi 2026, l'archivage de la saison (`teams`/`games`, page `tournois/[year]`) devra gérer le format utilisé — 3 formats coexistent désormais : `classique` (8 équipes + demies + 4 finales), `10_equipes` (2 poules de 5 + 5 finales classées) et `ronde` (système suisse 4 rondes + 5 finales classées, cf. `documents/rondes.md`) — les années auront des structures différentes selon le format choisi. Pas encore traité, à reprendre après la fin du tournoi 2026.
- [ ] **Photos :** Prévoir une politique de rétention/migration pour les photos uploadées avant l'introduction du système vignette + version complète (chemin plat, non listées par la galerie actuelle).

## 🕒 BACKLOG
- [ ] **Communication :** Système de push messages exclusif aux admins et super, avec affichage sur la première page.
- [ ] **Documentation :** Générer un fichier `notes.md` basé sur le `README.md` et les logs de commit GitHub.
- [ ] **Vidéos :** Tracking "vidéo lue" mis de côté (contrainte iframe YouTube cross-origin — nécessiterait l'API IFrame officielle de YouTube, hors scope pour l'instant).
- [ ] **Stockage :** Suivre l'usage du bucket `photos_import` (quota 1 Go / plan Supabase free) à mesure que la galerie grossit.

## 💡 BOÎTE À IDÉES (V2 / Futur)
- [ ] Système de commentaires internes sur les tâches pour les futurs collaborateurs (si extension).
- [ ] Export PDF automatique des rapports de progression.
- [ ] "Qui est en ligne" en vrai temps réel (Supabase Realtime Presence) plutôt que fenêtre glissante de 1h basée sur `activity_logs` — plus précis mais demande de brancher un canal de présence sur toute l'app.

---

##  IDÉES ABANDONNEES
- [ ] Optimisation des performances de chargement pour les gros volumes de données.
- [ ] Mise en place du système de backup automatisé.

---

## ✅ RÉALISATIONS
- [x] **Super :** Corrigé le bouton retour de `/live/charte` — icône X (au lieu du lien texte "Retour"), renvoie vers `/live/super` (au lieu de l'accueil).
- [x] **Admin** Créer la page d'administration des utilisateurs (table `site_users` Supabase) avec gestion des rôles (membre/admin/super) accessible uniquement par les "super".
- [x] **API/TODO&CHART** Développer l'endpoint `/api/dev/todo` pour lire le fichier MD local, done egalement pour la charte
- [x] **R/2026** Ajouter à la table `site_users` une relation avec le joueur préféré (tables `profiles` et `live_selected`). Colonne `favoris` créée dans `site_users` avec foreign_key vers `profiles`
- [x] **5/2026** Implémenter le middleware de restriction pour la page `/admin/todo` (accès réservé aux `super-members`).
- [x] **5/2026** Créer la page interne d'affichage du `TODO.md` (via `marked` ou `react-markdown`).
- [x] Choix de la stack technique (JS/MD).
- [x] Configuration du dépôt GitHub.
- [x] Connexion Google Keep <=> Flux de travail.
- [x] **Live :** Case à cocher "confirmé/payé" sur la sélection de joueurs (`live_selected.confirmed`), conservée entre déselection/reselection, remise à zéro par `reset_tournament`.
- [x] **Accueil :** Bandeau adaptatif date de l'événement (à venir / rappel si non-confirmés / programme du jour si tout le monde a confirmé / masqué après l'événement).
- [x] **Sécurité :** Comblé la faille d'inscription Google OAuth — le code d'invitation n'était vérifié que côté client et jamais recontrôlé au retour d'auth. Revérification serveur dans `auth/callback/route.ts`, suppression du compte non autorisé si invalide.
- [x] **Logs :** `session_logging` (connexions/déconnexions) déjà en place via `session_logs`.
- [x] **Métriques :** Système d'audit log complet — table `activity_logs`, helper `logActivity` (exclut le rôle super), tracking automatique des pages vues (`PageViewTracker`) et des actions admin/favoris/photos, page `/live/activity` (journal filtrable par section) et `/live/online` (qui est en ligne, fenêtre 1h).
- [x] **Stats :** Onglet "Popularité" (page/joueurs/tournoi/photo les plus consultés, via RPC `get_popularity_stats`), nouvelles vignettes records (points marqués/encaissés, fanny infligées/subies, victoires/défaites/nuls, défaites sur le fil), tri stable par winrate (bug de mutation d'array corrigé), effet de vague de lumière sur les vignettes.
- [x] **Médiathèque :** Restructuration `/videos` en hub (Vidéos/Photos/Contribuez) + galerie photo alimentée par les membres avec upload compressé (WebP, ~1,5 Mo, conversion côté navigateur) et miniatures dédiées pour limiter la bande passante.
- [x] **UI :** Navbar mobile passée en icônes (burger supprimé), barre de chargement globale, feedback tactile CSS global, correction des contrastes de gris trop sombres sur l'ensemble de l'app.
- [x] **Résidence :** Rapprochement nom/lot du Bâtiment B entre `data/residence.ts` et la convocation d'AG (`documents/private/`, non commité).
- [x] **Live :** Tableau de palmarès/résultats de poules unifié sur toutes les pages (poules, finale, podium, live public, archives `tournois/[year]`) via un composant partagé `PouleStandingsTable` + utilitaire `calculatePouleStandings` — colonnes V/D/N, Pour/Contre, Diff, Pts identiques partout (au lieu de versions "mini" appauvries ailleurs que sur la page poules).
- [x] **UI :** Cohérence tireur=orange/pointeur=violet appliquée sur ~10 fichiers (tournois, stats, SeasonHistory, tout le cycle live admin, PredictionModal, admin_teams) — plusieurs endroits affichaient les noms sans couleur ou avec une couleur incorrecte (rouge/blanc/bleu).
- [x] **Nettoyage :** Suppression des fichiers morts confirmés (audit du 27/07, 32 fichiers de `components/lib/utils/hooks` vérifiés un par un) : `PredictionModal-bayer.tsx`, `PredictionModal-cp1.tsx`, `PredictionModal-cp2.tsx`, `Logo_anc.tsx`, `AdminSettings.tsx`. `lib/auth-actions.ts` volontairement conservé pour revérification sécurité (cf. Priorités Critiques).
- [x] **Favoris :** Étoile favori (`components/FavoriStar.tsx` + `utils/favori.ts` serveur + `hooks/useFavoriId.ts` client) étendue à ~12 pages où un nom de joueur apparaît (au-delà de la seule fiche joueur) : stats, tournois, cycle live admin, admin_joueurs, GlobalProgressionChart. Non traité : `SeasonHistory.tsx` (le nom du partenaire n'a pas d'id de profil disponible dans les données actuelles) et les `<option>` natifs d'`admin_teams` (impossible d'y insérer une icône).
- [x] **Live :** Format **Ronde** (système suisse) ajouté comme 3ème option de tournoi à 10 équipes, à côté de `classique` et `10_equipes` — 4 rondes appariées par classement (anti-rematch), puis une 5ème ronde de finales classées réutilisant le mécanisme des finales du format `10_equipes` (types `Finale Rang1..5`, bonus ELO "Finale" hérité automatiquement). Nouvelle page `/live/ronde`, `Stepper.tsx` refactoré (`skipDemi` → `format`, pilote la liste d'étapes pour les 3 formats). Détail complet : `documents/rondes.md`. Mergé (PR #2).
- [x] **Live/Test :** Bouton "Simuler" (super admins) sur poules/demi/finale/ronde — remplit les matchs non terminés avec des scores aléatoires via le pipeline ELO normal (`utils/simulate.ts`), mode gaussien avec nuls autorisés pour les poules/rondes, mode décisif (13 pts) pour demis/finales. Nouveau hook `hooks/useIsSuper.ts`.
- [x] **Sécurité :** Réinitialisation de mot de passe pour les comptes pseudo `@pst.net` (`/reset-password`, `app/api/auth/reset-password/route.ts`) — le flux standard Supabase par email est inutilisable (adresse synthétique, pas de vraie boîte mail). Vérification par le code d'invitation existant (RPC `verify_invitation_code`, réutilisé) puis `admin.auth.admin.updateUserById`. **Point d'attention accepté** : le code d'invitation est partagé par tous les membres, donc n'importe qui le connaissant peut réinitialiser le mot de passe d'un autre membre s'il connaît son pseudo — risque jugé acceptable vu le contexte (club fermé), tracé via un log d'audit (`session_logs`/`PASSWORD_RESET`, visible dans `/live/activity`). Pas de rate-limiting. À revoir si le club grossit ou si un abus est constaté.
- [x] **Live :** Mode **"Tirage en direct"** (`live_tournament.team_mode`, orthogonal au format) pour la constitution des équipes à l'étape EQUIPES — alternative au mélange automatique historique. Nouveau composant `components/LiveDraftDraw.tsx` : le tirage au sort a lieu physiquement (boules, tombola), l'app se contente d'enregistrer le joueur cliqué parmi les restants (aucune randomisation côté app). Deux sous-modes : "Présélection P/T" (respecte les pools JOUEURS) ou "Rôles aléatoires" (pool unique, rôle alterné automatiquement par position de tirage, un seul réglage global). Bouton d'échange de rôle a posteriori (`swapRoles`) et verrou du lancement du tournoi tant que le draft n'est pas complet, ajoutés pour les deux modes. Corrections d'ergonomie associées : sélecteurs Format/Constitution fusionnés en une seule barre compacte sous l'en-tête (au lieu de 2 blocs pleine largeur empilés), grisage du format si la sélection en cours dépasse son quota, alerte visuelle (rouge/blanc) sur les compteurs Pointeurs/Tireurs en cas de dépassement, bouton "Actualiser" ajouté sur `admin/page.tsx` (et sur poules/demi/finale/ronde/podium) faute de Realtime sur ces pages.
- [x] **Concept :** Page publique `/concept` mise à jour pour présenter les formats "10 équipes" et "Ronde" comme options possibles quand le nombre d'équipes dépasse 8, à la suite du règlement du format classique.
- [x] **Résidence :** Espace réservé (`super` uniquement pour l'instant) sous `/render/prive` — Conseil Syndical (`residence_contacts`, coordonnées + lien optionnel vers un n° d'appartement), bibliothèque de documents PDF (`residence_documents` + bucket Storage privé du même nom, URL signées), codes d'accès de la résidence (`residence_codes`, portails/digicodes, bouton copier). Gate d'accès dédiée (`render/prive/layout.tsx`, même principe que `live/(super)/layout.tsx`), point d'entrée conditionné par `userRole === 'super'` sur `/render`. Jeu de données initial du Conseil Syndical extrait de `documents/private/Extranet Reveille.pdf`. Colonne `access_level` prévue sur les 3 tables pour une extension future "membre privilégié" sans migration de schéma. **Nécessite l'exécution manuelle du SQL fourni (tables + policies RLS + bucket) dans Supabase — voir `documents/architecture.md` §5/§9.**
- [x] **Résidence :** Suite de l'espace réservé — 5 catégories de contacts filtrables (Conseil Syndical, Gardien, Copropriétaire, Locataire, **Fournisseur**), nouveau champ `contrat` (prestation/référence, catégorie Fournisseur), 27 fournisseurs extraits du même PDF (`documents/private/seed_fournisseurs.sql`, non versionné). Téléphone/email en liens `tel:`/`mailto:` (ouverture directe Téléphone/Mail sur iPhone). Bouton **Partager** par contact (vCard `.vcf` + `navigator.share`, feuille de partage native iOS), rendu toujours visible (pas seulement au survol, pour l'usage tactile). Intégration croisée : la fiche double-clic d'un appartement sur la scène 3D publique affiche désormais le tél/email du contact lié par `apartment_num` (mode super) ; le gizmo XYZ (hover, mode super) est masqué pour l'instant. Le panel super rapide `/live/super` affiche les `residence_codes` en tête de page (tap pour copier, optimisé iPhone) en plus du lien de gestion complet.
- [x] **Résidence :** Documents — abandon de l'upload binaire Supabase Storage au profit de liens Google Drive (`residence_documents.external_url`) : certains PDF de la résidence dépassent 50 Mo, hors de portée pratique du quota gratuit Supabase (1 Go, déjà partagé avec `photos_import`). Ajout d'une catégorisation (Syndic, Fournisseurs, AG, PV, Autre — filtrable par pills). Champ **résumé** markdown optionnel par document (panneau dédié, aperçu via `MarkdownDisplay`, tableaux corrigés par l'ajout de `remark-gfm` — absent jusque-là, les tableaux markdown s'affichaient en texte brut avec les `|` littéraux). **Nécessite l'exécution manuelle du SQL fourni dans Supabase** (`documents/private/migrate_documents_to_drive.sql` puis `add_document_category_visibility.sql` puis `add_document_resume.sql`).
- [x] **Résidence :** Remplacement du modèle public/private (une itération précédente, jugée trop grossière) par un **contrôle d'accès par palier et par utilisateur** — nouvelle colonne `site_users.residence_access_level` (0 aucun, 1 Consultation, 2 Avancé, `super` toujours illimité), gérée depuis `/live/users`. RPC `get_residence_access_level()` (même principe que `is_super()`), hook `hooks/useResidenceAccessLevel.ts`, gate serveur factorisée `lib/residence-access.ts#hasResidenceAccess()`. Nouvelles pages de lecture seule `render/documents/`, `render/codes/` (palier ≥ 1) et `render/contacts/` (palier ≥ 2), chacune gardée par son `layout.tsx`. Cas particulier du résumé markdown (visible seulement au palier 2, alors que le reste du document l'est dès le palier 1) résolu par une vue Postgres `residence_documents_public` (`security_invoker = true`) qui redacte la colonne — RLS ne pouvant filtrer qu'au niveau de la ligne, pas de la colonne. Nettoyage : colonnes `visibility`/`access_level` (devenues obsolètes) supprimées des 3 tables. **Nécessite l'exécution manuelle de `documents/private/residence_access_levels.sql`.**
- [x] **Résidence :** Fiches signalétiques des 173 lots du Bâtiment B (`residence_lots`), importées depuis `documents/private/batiment_B_lots.json` (état descriptif de division, acte notarié de 1957 — schéma uniforme par catégorie : composition, tantièmes, étage, situation...). Correspondance arithmétique (pas de rapprochement flou) avec `data/residence.ts` établie et vérifiée pendant cette session : `identifiant_local` "B12"/"Box 11"/"Chambre de bonne 1" ↔ `num` 12/11/1 dans `apartments[]`/`garages[]`/`chambresDeBonne[]` — 110 des 173 lots liés (studios, appartements, box, chambres de bonne), 63 sans contrepartie 3D (caves, placards, non modélisés individuellement). Au passage, correction d'une confusion sur `data/residence.ts` : il ne modélise que le **Bâtiment B** (pas de Bâtiment A), le libellé `"sectionB"` dans les données ne correspondant à rien de légal — juste les deux moitiés architecturales du même bâtiment. Fiche complète affichée au double-clic dans la scène 3D (palier ≥ 2, extension du panneau d'info existant pour les 3 types sélectionnables : appartement/garage/CdB) et nouvelle page de recherche `render/lots/` (filtre par catégorie, recherche par identifiant) pour l'ensemble des 173 lots. Pas de CRUD (donnée figée) : import unique via `documents/private/residence_lots.sql` (généré depuis le JSON, non versionné), palier de lecture 2 comme les contacts.
- [x] **Résidence :** Ajout des 48 lots du Bâtiment A (`documents/private/batiment_A_lots.json`, même acte notarié — 20 appartements, 6 studios, 10 box, 12 caves) dans `residence_lots` — table déjà en place, seed additionnel seul (`residence_lots_batiment_A.sql`, `plan_kind`/`plan_num` toujours `null` puisque le Bâtiment A n'est pas modélisé en 3D). Total résidence : 221 lots (2873 + 7127 = 10000/10000 de tantièmes, validation croisée confirmée dans les métadonnées source). `render/lots/` dispose désormais d'un filtre Bâtiment (Tous/A/B) au-dessus du filtre par catégorie, badge bâtiment affiché sur chaque ligne quand "Tous" est sélectionné.
- [x] **Résidence :** Ajout de la **loge du gardien** (Bâtiment A) comme nouvelle catégorie `partie_commune` — locaux mentionnés dans la désignation générale de l'acte notarié mais jamais individualisés en lot privatif numéroté ni dotés de tantièmes. `residence_lots.numero_lot` passé en `nullable` pour ce cas (`documents/private/residence_add_loge_gardien.sql`, alter + insert), `render/lots/` reconnaît la nouvelle catégorie (icône/couleur dédiées) et affiche "—" quand `numero_lot` est absent.
- [x] **Live :** Attribution des **terrains T1 à T4** sur les matchs de poules du format `10_equipes` — nouvelle colonne `live_matches.terrain`, renseignée à la génération (`admin/page.tsx`) via une table figée (`POULE5_COURTS`). Demande initiale ("chaque équipe joue une fois sur chaque terrain") mathématiquement irréalisable avec 4 terrains pour une poule de 5 équipes (round-robin = K5, nombre chromatique d'arêtes = 5, prouvé par recherche exhaustive) : chaque équipe joue 4 matchs et ne peut couvrir que 3 terrains distincts sur 4 au mieux. Table retenue = meilleure répartition trouvée (les 5 équipes couvrent chacune exactement 3 terrains, usage des 4 terrains équilibré 3/3/2/2). Badge terrain affiché sur `poules/page.tsx` (admin) et `app/live/page.tsx` (public). **Nécessite l'exécution manuelle de `documents/private/add_live_matches_terrain.sql`.** Répartition des poules simplifiée dans la foulée : `getGassinIds('10_equipes')` passé de l'alternance `A,C,E,G,I` à deux blocs contigus `A-E` (Gassin) / `F-J` (Ramatuelle) — la logique de terrains reste inchangée (indices locaux 0..4 dans le tableau de la poule, indépendants des lettres réelles).
- [x] **Live :** Correction de l'attribution des terrains — la table initiale ignorait que les 2 poules jouent **en parallèle sur les mêmes 4 terrains physiques** (à chaque ronde, 2 matchs Gassin + 2 matchs Ramatuelle simultanés), ce qui pouvait faire tomber un match Gassin et un match Ramatuelle sur le même terrain au même moment. `POULE5_COURTS` remplacée par une table par poule (`.Gassin`/`.Ramatuelle`), calculée conjointement pour qu'à chaque ronde les 4 terrains soient utilisés une fois chacun sans collision entre poules — garantie conservée : les 10 équipes couvrent chacune au moins 3 terrains sur 4. Découverte en préparant le tableau papier d'organisation du tournoi. Réordonnancement des tableaux locaux (`gassinRR`/`ramatuelleRR`) pour que **E** et **J** soient systématiquement les premières équipes au repos (ronde 1) — la structure round-robin met toujours l'équipe d'index local 2 au repos en 1ère ronde, indépendamment des lettres.
